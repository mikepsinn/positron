The contents of the modified ".github/workflows/pr_review.yml" file:

name: Pull Request Review

on:
  pull_request_review:
    types:
      - submitted
      - edited

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.x

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Analyze pull request review
        run: python agents/pr_review_analyzer.py

      - name: Handle pull request review
        run: python agents/pr_review_handler.py
